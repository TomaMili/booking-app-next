import Counter from "../_components/Counter";

export const metadata = {
  title: "Rooms",
};

export default function Page() {
  return (
    <div>
      <h1>Rooms page</h1>

      <Counter />
    </div>
  );
}
